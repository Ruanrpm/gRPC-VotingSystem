# gRPC-VotingSystem
A distributed voting system developed in C++ using gRPC and Protocol Buffers. The project demonstrates remote procedure calls, client-server communication, concurrent requests, shared server-side state, and execution across multiple machines on a local network.
